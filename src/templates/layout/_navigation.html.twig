<!-- Navigation-->
<nav class="navbar navbar-expand-lg navbar-light bg-white py-3">
    <div class="container px-5">
        <a class="navbar-brand" href="{{ path('/') }}">
            <span class="fw-bolder text-primary">Xavier's Blog</span>
        </a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <ul class="navbar-nav ms-auto mb-2 mb-lg-0 small fw-bolder">
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('/') }}">Home</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('/posts') }}">Blog Posts</a>
                </li>
                {% if is_granted('ROLE_EDITOR') %}
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('/admin') }}">Admin</a>
                    </li>
                {% endif %}
                <li class="nav-item ms-3">
                    {% if is_granted('ROLE_USER') %}
                        <a class="btn btn-outline-danger" href="{{ path('/logout') }}">Logout</a>
                    {% else %}
                        <a class="btn btn-outline-primary" href="{{ path('/login') }}">Login</a>
                    {% endif %}
                </li>
            </ul>
        </div>
    </div>
</nav>
