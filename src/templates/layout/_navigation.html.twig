<!-- Navigation-->
<nav class="navbar navbar-expand-lg navbar-light bg-white py-3">
    <div class="container px-5">
        <a class="navbar-brand" href="{{ path('home') }}">
            <span class="fw-bolder text-primary">Xavier's Blog</span>
        </a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <ul class="navbar-nav ms-auto mb-2 mb-lg-0 small fw-bolder">
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('home') }}">Home</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('posts') }}">Blog Posts</a>
                </li>
                {% if not is_granted('ROLE_USER') %}
                    <li class="nav-item">
                        <a class="btn btn-primary" style="font-weight: bold;" href="{{ path('login') }}">Sign In</a>
                    </li>
                {% else %}
                    {% if is_granted('ROLE_ADMIN') %}
                        <li class="nav-item">
                            <a class="nav-link" href="{{ path('admin_dashboard') }}">Dashboard</a>
                        </li>
                    {% endif %}
                    <li class="nav-item">
                        <a class="btn btn-primary" style="font-weight: bold;" href="{{ path('logout') }}">Sign Out</a>
                    </li>
                {% endif %}
            </ul>
        </div>
    </div>
</nav>
